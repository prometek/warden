[package]
name = "warden-policy"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Declarative policy engine for allowing, denying, or requiring approval for Warden actions."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9.34"
thiserror = "2"
